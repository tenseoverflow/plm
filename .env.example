# Database
DATABASE_URL=postgresql://username:password@localhost:5432/plm

# Server
PORT=3000
NODE_ENV=development

# Passkey configuration
RP_NAME=PLM
RP_ID=localhost
ORIGIN=http://localhost:5173

# Optional: JWT secret for sessions (if you want to add JWT auth later)
JWT_SECRET=your-secret-key-here
